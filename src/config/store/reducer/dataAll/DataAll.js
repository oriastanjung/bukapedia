import { createSlice } from "@reduxjs/toolkit"

const initialState = [
    dataAll = 0
]

const AritmaticLogic = createSlice({
    name: "art",
    initialState,
    reducers: {

    }
})

export const {} = AritmaticLogic.actions
export default AritmaticLogic.reducer